name: AI UI Generator

on:
  workflow_dispatch:
    inputs:
      component_name:
        description: 'Component to generate'
        required: true
        type: string

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Component Name
        run: echo "Generating component ${{ github.event.inputs.component_name }}"
